{
  "name": "UI devcontainer",
  "dockerComposeFile": "docker-compose.yml",
  "service": "repo",
  "containerUser": "root",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "features": {},
  "postCreateCommand": "npm update --global npm && npm install && npm --prefix ./client install && npm --prefix ./server install && npm --prefix ./tests install",
  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode"
      ]
    }
  }
}