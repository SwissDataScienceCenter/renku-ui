import displaySlice, {
  showSshModal,
  hideSshModal,
  toggleSshModal,
  useDisplaySelector,
  reset,
} from "./displaySlice";
import { Display, ProjectConfig } from "./display";

export {
  displaySlice,
  showSshModal,
  hideSshModal,
  toggleSshModal,
  useDisplaySelector,
  reset,
};
export type { Display, ProjectConfig };
